package com.ERP.QMS.controller;

// This controller's REST endpoints have been migrated:
//   Document Issue Register → DocumentIssueController  (/document-issues)
//   Quality Issues           → QualityIssueController   (/issue-register)
//
// This file is kept as a placeholder so existing service injections compile.
// It is NOT registered as a @RestController — no duplicate mapping.

// If you need to restore document-issue routing from /issue-register,
// re-add @RestController and @RequestMapping("/issue-register") here
// and remove QualityIssueController.

public class IssueRegisterController {
    // Intentionally empty — see DocumentIssueController and QualityIssueController
}
